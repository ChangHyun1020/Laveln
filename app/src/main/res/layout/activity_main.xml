<?xml version="1.0" encoding="utf-8"?><!--
    activity_main.xml
    ─────────────────────────────────────────────────────────
    변경사항:
      Toolbar 타이틀 제거
      → Toolbar 안에 앱 이름(작게) + SearchView 를 함께 배치
      → 검색바가 항상 보이는 상시 검색 구조
      사이드바 메뉴 추가 (DrawerLayout)

    기존과 달라진 점:
      SearchView: gone → 항상 visible, Toolbar 안에 배치
      FAB: 팝업 메뉴 없이 바로 AddVesselActivity 이동
      최상위 레이아웃을 DrawerLayout으로 변경
    ─────────────────────────────────────────────────────────
-->
<androidx.drawerlayout.widget.DrawerLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/drawerLayout"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:fitsSystemWindows="false"
    android:background="@color/background_light">

    <!-- 메인 컨텐츠 영역 -->
    <androidx.coordinatorlayout.widget.CoordinatorLayout
        android:id="@+id/mainLayout"
        android:layout_width="match_parent"
        android:layout_height="match_parent">

        <com.google.android.material.appbar.AppBarLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            app:elevation="0dp"
            android:backgroundTint="?attr/colorPrimary"
            android:id="@+id/appBarId">

            <androidx.appcompat.widget.Toolbar
                android:id="@+id/toolbar"
                android:layout_width="match_parent"
                android:layout_height="?attr/actionBarSize"
                android:background="?attr/colorPrimary">

                <!--
                    Toolbar 내부 레이아웃:
                    사이드바 메뉴 아이콘 + 앱 이름(고정) + 검색바(나머지 공간 전부 차지)
                -->
                <LinearLayout
                    android:layout_width="match_parent"
                    android:layout_height="match_parent"
                    android:orientation="horizontal"
                    android:gravity="center_vertical"
                    android:paddingEnd="8dp"
                    tools:ignore="RtlSymmetry">

                    <!-- 메뉴 아이콘: DrawerLayout 열기 -->
                    <ImageView
                        android:id="@+id/ivMenu"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:src="@android:drawable/ic_menu_sort_by_size"
                        android:contentDescription="@string/nav_menu_open"
                        android:layout_marginEnd="8dp"
                        android:layout_marginTop="10dp"
                        app:tint="#FFFFFF" />

                    <!-- 앱 이름: 고정 너비 -->
                    <TextView
                        android:id="@+id/topName"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:layout_gravity="center"
                        android:layout_marginEnd="8dp"
                        android:text="@string/main_banner"
                        android:textColor="#FFFFFF"
                        android:textSize="20sp"
                        android:textStyle="bold"
                        android:singleLine="true"
                        android:layout_marginTop="10dp" />

                    <!--
                        SearchView: 남은 공간 전부 차지
                    -->
                    <androidx.appcompat.widget.SearchView
                        android:id="@+id/searchView"
                        android:layout_width="0dp"
                        android:layout_height="wrap_content"
                        android:layout_gravity="top"
                        android:layout_weight="1"
                        app:iconifiedByDefault="false"
                        app:queryHint="@string/search_hint"
                        app:queryBackground="@drawable/search_bar_background"
                        app:searchIcon="@android:drawable/ic_menu_search"
                        android:layout_marginTop="10dp"
                        android:visibility="gone" />

                </LinearLayout>
            </androidx.appcompat.widget.Toolbar>

        </com.google.android.material.appbar.AppBarLayout>

        <!-- ── 통합 프래그먼트 호스트 (상단 그래프 + 하단 리스트) ────────────────────── -->
        <androidx.fragment.app.FragmentContainerView
            android:id="@+id/fragmentContainer"
            android:name="com.example.vesselv2.ui.fragment.VesselCombinedFragment"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:clipToPadding="false"
            android:paddingTop="8dp"
            android:paddingBottom="80dp"
            app:layout_behavior="@string/appbar_scrolling_view_behavior" />

        <!-- FAB: 모선 등록 -->
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fab"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="16dp"
            android:contentDescription="@string/add_vessel_desc"
            app:srcCompat="@android:drawable/ic_input_add"
            android:visibility="gone" />

    </androidx.coordinatorlayout.widget.CoordinatorLayout>

    <!-- 사이드 메뉴(Navigation Drawer) -->
    <com.google.android.material.navigation.NavigationView
        android:id="@+id/navigationView"
        android:layout_width="wrap_content"
        android:layout_height="match_parent"
        android:layout_gravity="start"
        android:fitsSystemWindows="true"
        app:headerLayout="@layout/nav_header_main"
        app:itemIconTint="@color/md_primary"
        app:itemTextColor="@color/text_secondary"
        app:menu="@menu/nav_menu" />

</androidx.drawerlayout.widget.DrawerLayout>
